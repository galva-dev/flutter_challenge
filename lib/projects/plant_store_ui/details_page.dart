import 'package:flutter/material.dart';

class DetailsPagePlantUI extends StatelessWidget {
  const DetailsPagePlantUI({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}